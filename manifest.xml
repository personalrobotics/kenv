<package>
    <description brief="2D kinematic simulation environment.">
        Two-dimensional kinematic simulation environment where objects are
        represented using GEOS geometry primitives. This code also supports the
        analytic computation of a polyhedral approximation of C-space
        obstacles.
    </description>
    <author>Michael Koval</author>
    <rosdep name="cgal"/>
    <rosdep name="eigen"/>
    <rosdep name="yaml-cpp"/>
    <!-- TODO: This should also depend on the "geos" system library. -->
    <depend package="kenv"/>
    <export>
        <cpp cflags="-I${prefix}/include" lflags="-L${prefix}/lib -lpolygonal_kenv"/>
        <python path="${prefix}/pythonsrc"/>
    </export>
</package>
